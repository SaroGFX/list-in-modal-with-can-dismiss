import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ListInModal with canDismiss',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
